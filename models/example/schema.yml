
version: 2

models:
    - name: calendar_date
      description: "Calendar date"
      columns:
          - name: cal_date
            description: "The primary key for this table"
            tests:
                - unique
                - not_null
